version: 2
jobs:
  build:
    docker: 
      - image: circleci/node:4.8.2 # the primary container, where your job's commands are run
    steps:
      - checkout
      - run:
          npm run test
      - store_artifacts:
          path: /tmp/artifacts
          destination: build